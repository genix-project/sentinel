# specify path to genix.conf or leave blank
# default is the same as GenixCore
#genix_conf=/home/evan82/.genixcore/genix.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
